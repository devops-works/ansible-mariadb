---
collections:
  # community.mysql is deprecated (modules removed in 6.0.0), and its
  # replacement ansible.mysql drops MariaDB support in *its* 6.0.0.
  # ansible.mariadb is the MariaDB-specific successor.
  - name: ansible.mariadb
    version: ">=6.0.0"
